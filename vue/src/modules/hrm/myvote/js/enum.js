const breakVoteType = {
    
    inactive: {
        waiting: "Đang chờ",
        approved: "Dẫ duyệt",
        destroy: "Đã hủy"
    }
};

const VoteEnum = {
    breakVoteType
};

export default VoteEnum;
